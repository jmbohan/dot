We need to remember to use the config alias for config files using the
bare directory for git 

# So instead of using: # 

`git add` or `git commit` , `git push` 

## Use: ##

`config add` or `config commit`, `config push`


